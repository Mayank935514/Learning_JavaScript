/** Iterating over String */

let displayMessage = "How are you?";
for(let char of displayMessage)
{
    console.log(char);
}

// Or we can write //
let message = " How is";
for(let i=0; i<message.length;i++)
{
    console.log(message[i]);
}