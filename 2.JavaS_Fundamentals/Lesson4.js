/** Ternary operator -->condition ? yes : no */

const totalMarks = 60;

(totalMarks < 40) ? console.log("you need to work hard") : console.log("You don't need to do work hard")

const result = totalMarks < 40 ? "You need to do work hard" : "You don't need to do work hard"
console.log(result);