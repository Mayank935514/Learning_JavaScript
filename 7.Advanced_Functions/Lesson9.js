/**
 * Global and Local Variables
 * Global, Local and Block Scope
 */

// var: Variables declared with var are either globally scoped or function scoped, meaning they do not adhere to block scope. 
//      If declared inside a block, they are still accessible outside the block.


// let and const: Variables declared with let or const are block scoped, 
//                meaning they are only accessible within the block where they are defined.
