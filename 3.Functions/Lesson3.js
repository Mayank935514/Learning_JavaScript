/** Function return */

function calculateSum(x,y)
{
    return x + y;        // Return the Sum value
}

const result = calculateSum(2,3);   //After calculating the return value comes where function is called
console.log(result);